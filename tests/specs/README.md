Curated end-to-end specs that are part of the active WDIO suite should live here and use the `.e2e.js` suffix.

`bootstrap.e2e.js` is a minimal infrastructure smoke test and can remain as a quick session check.

Generated artifacts now live under `generated-tests/` so they can be regenerated without affecting the default execution path.
